# Dead Letter Channel IoT Sensor Data Processing Example

# Camel configuration
camel.main.name = IoTSensorProcessingWithDeadLetterChannel
camel.main.duration-max-seconds = 60
camel.main.shutdown-timeout = 30

# Application metadata
app.name = Dead Letter Channel EIP Example
app.description = IoT Sensor Data Processing with Dead Letter Channel for Error Handling
app.version = 1.0.0
